# Este é um arquivo de exemplo para as variáveis de ambiente
# Copie este arquivo para .env e ajuste os valores conforme necessário

# Ambiente (development/production)
NODE_ENV=development

# URLs do Backend
REACT_APP_BACKEND_URL=http://localhost:3002
REACT_APP_SOCKET_URL=http://localhost:3002

# Configurações do Socket.IO
REACT_APP_SOCKET_PATH=/socket.io
REACT_APP_SOCKET_RECONNECTION_ATTEMPTS=10
REACT_APP_SOCKET_TIMEOUT=20000

# URLs permitidas em desenvolvimento
REACT_APP_DEV_ORIGINS=["http://localhost:3000","http://localhost:5002"]

# URLs permitidas em produção
REACT_APP_PROD_ORIGINS=["https://app.timeboard.site","https://timeboard.site"]

# Observações:
# 1. Em produção, use HTTPS para todas as URLs
# 2. Certifique-se de que as origens correspondam às configurações CORS do backend
# 3. O NODE_ENV será automaticamente definido como 'production' durante o build

# Porta do backend
PORT=5001

# Email Configuration
EMAIL_USER=seu_email@exemplo.com
EMAIL_PASS=sua_senha_de_app

# EmailJS Configuration
REACT_APP_EMAILJS_PUBLIC_KEY=YOUR_PUBLIC_KEY
REACT_APP_EMAILJS_SERVICE_ID=YOUR_SERVICE_ID
REACT_APP_EMAILJS_TEMPLATE_ID=YOUR_TEMPLATE_ID 